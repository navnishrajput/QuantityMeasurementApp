# UC16: H2 Database Configuration (Development/Testing)
spring.datasource.url=jdbc:h2:mem:quantitydb;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# UC16: MySQL Database Configuration (Production - Commented)
# spring.datasource.url=jdbc:mysql://localhost:3306/quantitydb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=MySQL#Nav@2025
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# UC16: Connection Pool Configuration
pool.max.size=10
pool.min.idle=2
pool.timeout.ms=30000
pool.idle.timeout.ms=60000

# UC16: Repository Configuration
repository.type=database

# UC16: Logging Configuration
logging.level.com.quantity.measurement=DEBUG
logging.level.com.quantity.measurement.repository=DEBUG
logging.level.com.quantity.measurement.service=INFO
logging.level.com.quantity.measurement.controller=INFO